export enum ESort {
  ASC = 1,
  DESC = -1
}

export enum EPagingDefaults {
  pageIndex = 1,
  pageSize = 10
}

export enum EFireBaseFolder {
  USERS = 'users',
  PRODUCTS = 'products',
  BRANDS = 'brands',
}
